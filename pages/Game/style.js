import {StyleSheet} from "react-native";

export default StyleSheet.create({
    bgImage:{
        flex:1,
        alignContent:"center",
        alignItems:"center",
        padding:50
    },
    container:{
        display: "flex",
        flexWrap:"wrap",
        flexDirection: "row",
        justifyContent:'space-between',
    }
});